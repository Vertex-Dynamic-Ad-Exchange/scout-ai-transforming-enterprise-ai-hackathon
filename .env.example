# Scout — env var manifest
#
# Skeleton: empty. Real vars (GEMINI_API_KEY, BROWSER_USE_API_KEY,
# LOBSTERTRAP_BASE_URL, REDIS_URL, etc.) land alongside the packages that
# read them. Each package will own its own `src/config.ts` and document its
# vars here.
